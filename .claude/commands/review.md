Run a PR review on the current branch using @pr-reviewer:

1. Confirm the current branch is not `main`
2. Delegate to the `@pr-reviewer` agent to review all changes compared to `main`
3. Report the findings back
