Check translation files for issues:

1. Read `src/i18n/en.json` and `src/i18n/es.json`
2. Compare keys between both files — report any keys present in one but missing in the other
3. Search all `.tsx` files for `t('` calls and extract the translation keys being used
4. Report any keys used in code that are missing from the translation files
5. Report any keys in translation files that are not used in any component
6. If everything is consistent, confirm all translations are in sync
