Review recent changes to the codebase and update CLAUDE.md accordingly:

1. Check `git diff` and `git log` for recent changes since CLAUDE.md was last updated
2. Read the current CLAUDE.md
3. Update CLAUDE.md to reflect any new or changed:
   - Architecture (new files, modules, patterns)
   - Dependencies or tooling
   - Commands
   - Domain model changes
4. Do not remove existing content unless it is outdated or incorrect
5. Keep the file concise — avoid redundancy
