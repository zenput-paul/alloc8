Run tests with Vitest:

1. Run `npx vitest run $ARGUMENTS`
   - If no arguments provided, run all tests
   - If a file path or pattern is provided, run only matching tests
2. Report a summary of passed/failed tests. If any fail, show the failure details.
